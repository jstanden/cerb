---
id: "docs-tasks"
title: "Tasks"
url: "https://cerb.ai/docs/tasks/"
summary: "This page provides an overview of tasks in Cerb, describing them as individual units of work designated for a single worker. Tasks can include optional features such as a due date and an importance rating. Each task has a profile page that acts like a mini-project, complete with widgets, comment threads, attachments, and record links. When a ticket involves multiple simultaneous actions by different workers, separate tasks can be created for each action and linked to the same ticket. Tasks, similar to tickets, can have statuses of open, waiting, or closed."
tags: ["docs"]
---
**Tasks** are units of work that should be handled by a single worker. They may include an optional **due date** and **importance** rating. A task's profile page functions as a miniature project; with widgets, comment threads, attachments, and record links.

If a ticket requires simultaneous action items from multiple workers, a task can be created for each action and linked to the same ticket.

Just like tickets, a task's **status** can be open, waiting, or closed.

