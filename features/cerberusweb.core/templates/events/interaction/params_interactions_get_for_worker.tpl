<fieldset style="margin-top:5px;">
	<b>Listen on these interaction points:</b> (one per line, <tt>*</tt> for wildcards)
	<br>
	<textarea name="event_params[listen_points]" rows="6" cols="45" style="height:10em;width:100%;" placeholder="e.g. record:cerberusweb.contexts.ticket; record:*">{$trigger->event_params.listen_points}</textarea>
</fieldset>